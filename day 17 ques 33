Q33: Write a program to check if a number is an Armstrong number.

/*
Sample Test Cases:
Input 1:
153
Output 1:
Armstrong

Input 2:
123
Output 2:
Not Armstrong

*/

  #include <stdio.h>
#include<math.h>
int main () {
   int n,remainder,digits=0,result=0;
   printf("enter a number\n");
   scanf("%d",&n);

   while(n!=0){
      n = n/10;
      digits++;
   }
   while(n!=0) {
      remainder = n%10;
      result =result +pow(remainder,digits);
      n=n/10;
   }
   if(n==result){
      printf("it is a armstrong number\n");
   }
   else {
      printf("it is not a armstrong\n");
   }
   return 0;
}
